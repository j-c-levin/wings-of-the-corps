import type { GameState } from './types'
import type { Rng } from './rng'

/** Stub — Task 8 (war clock, finale, endings) fills this in. */
export function tickWar(_state: GameState, _rng: Rng): void {}
