import type { GameState } from './types'
import type { Rng } from './rng'

/** Stub — Task 6 (auction state machine) fills this in. */
export function tickAuction(_state: GameState, _rng: Rng): void {}
